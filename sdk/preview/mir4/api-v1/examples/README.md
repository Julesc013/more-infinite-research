# MIR 4 API V1 examples

The PowerShell and Python readers parse, validate, decode availability, and report the immutable response digest. Run them against files in ../vectors. Neither example mutates the player package or grants release authority.
