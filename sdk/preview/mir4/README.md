# MIR 4 SDK V0 preview

Run `.\conformance\Invoke-MIR4SdkConformance.ps1` with PowerShell 7. API bindings are under `api-v0`; MEP bindings are under `powershell` and `lua`; deterministic examples and shadow reports are under `reference`. This preview is read-only, package-excluded, and may change before 1.0.
