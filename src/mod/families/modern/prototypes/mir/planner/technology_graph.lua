-- Planner adapter retained for callers; the graph algorithm is owned by the
-- shared graph kernel and is also used for actual post-emission verification.
return require("prototypes.mir.graph.qualification")
