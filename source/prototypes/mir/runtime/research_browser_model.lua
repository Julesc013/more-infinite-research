-- Compatibility name for internal callers that predate the portable split.
-- The canonical implementation is research_browser_core.lua.
return require("prototypes.mir.runtime.research_browser_core")
