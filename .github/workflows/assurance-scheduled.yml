name: Assurance Scheduled Breadth

on:
  schedule:
    - cron: "17 3 * * 2"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  plan:
    name: verification-plan
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/cache/restore@v4
        with:
          path: .work/artifacts/assurance/evidence
          key: mir-ledger-${{ runner.os }}-2.1-${{ github.run_id }}
          restore-keys: |
            mir-ledger-${{ runner.os }}-2.1-
      - name: Build exact candidate
        shell: pwsh
        run: ./tools/mir.ps1 assurance build --target 2.1
      - name: Materialize forced breadth plan
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
          MIR_PRIOR_RELEASE: ${{ secrets.MIR_PRIOR_RELEASE_3_1_9 }}
        run: ./tools/mir.ps1 verify plan --target 2.1 --profile full --factorio $env:FACTORIO_BIN --prior $env:MIR_PRIOR_RELEASE --no-reuse --output .work/output/verification-plan.json
      - id: matrix
        shell: pwsh
        run: |
          $plan = Get-Content -Raw -LiteralPath .work/output/verification-plan.json | ConvertFrom-Json
          "matrix=$(@{ include = @($plan.work) } | ConvertTo-Json -Depth 10 -Compress)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      - uses: actions/upload-artifact@v4
        with:
          include-hidden-files: true
          name: mir-scheduled-plan-and-candidate
          path: |
            .work/output/verification-plan.json
            dist/*.zip
          if-no-files-found: error

  tests:
    name: ${{ matrix.test_id }}
    needs: plan
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.plan.outputs.matrix) }}
    concurrency:
      group: mir-${{ matrix.safe_test_id }}-${{ matrix.fingerprint }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: mir-scheduled-plan-and-candidate
          path: .
      - uses: actions/cache/restore@v4
        with:
          path: .work/artifacts/assurance/evidence
          key: mir-ledger-${{ runner.os }}-2.1-${{ github.run_id }}
          restore-keys: |
            mir-ledger-${{ runner.os }}-2.1-
      - name: Force exact test instance
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
          MIR_PRIOR_RELEASE: ${{ secrets.MIR_PRIOR_RELEASE_3_1_9 }}
        run: ./tools/mir.ps1 verify run-one --target 2.1 --plan .work/output/verification-plan.json --test '${{ matrix.test_id }}' --fingerprint '${{ matrix.fingerprint }}' --factorio $env:FACTORIO_BIN --prior $env:MIR_PRIOR_RELEASE --no-reuse
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          include-hidden-files: true
          name: mir-scheduled-evidence-${{ matrix.safe_test_id }}
          path: .work/artifacts/assurance/evidence
          if-no-files-found: warn

  verification-gate:
    name: verification-gate
    if: always()
    needs:
      - plan
      - tests
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: mir-scheduled-plan-and-candidate
          path: .
      - uses: actions/cache/restore@v4
        with:
          path: .work/artifacts/assurance/evidence
          key: mir-ledger-${{ runner.os }}-2.1-${{ github.run_id }}
          restore-keys: |
            mir-ledger-${{ runner.os }}-2.1-
      - name: Merge worker evidence
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          pattern: mir-scheduled-evidence-*
          path: .work/artifacts/assurance/evidence
          merge-multiple: true
      - name: Aggregate forced breadth gate
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
          MIR_PRIOR_RELEASE: ${{ secrets.MIR_PRIOR_RELEASE_3_1_9 }}
        run: ./tools/mir.ps1 verify gate --target 2.1 --plan .work/output/verification-plan.json --factorio $env:FACTORIO_BIN --prior $env:MIR_PRIOR_RELEASE --output .work/artifacts/assurance/scheduled-summary.json
      - uses: actions/cache/save@v4
        if: always()
        with:
          path: .work/artifacts/assurance/evidence
          key: mir-ledger-${{ runner.os }}-2.1-${{ github.run_id }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          include-hidden-files: true
          name: assurance-scheduled-2.1-${{ github.sha }}
          path: |
            .work/output/verification-plan.json
            .work/artifacts/assurance/evidence-bundle.json
            .work/artifacts/assurance/scheduled-summary.json
          if-no-files-found: warn
