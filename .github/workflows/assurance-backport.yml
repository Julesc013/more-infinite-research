name: Assurance Backport Matrix

on:
  workflow_dispatch:
    inputs:
      candidate_sha:
        description: "Trusted backport source commit"
        required: true
      baseline_sha:
        description: "Published target baseline"
        required: true

permissions:
  contents: read

jobs:
  plan:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        target: ["2.1", "2.0"]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ inputs.candidate_sha }}
          fetch-depth: 0
      - name: Materialize target plan
        shell: pwsh
        run: ./tools/mir.ps1 assurance backport --target '${{ matrix.target }}' --baseline '${{ inputs.baseline_sha }}' --output build/results/assurance/backport-${{ matrix.target }}.json
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          include-hidden-files: true
          name: assurance-backport-plan-${{ matrix.target }}-${{ inputs.candidate_sha }}
          path: build/results/assurance/**
          if-no-files-found: error
