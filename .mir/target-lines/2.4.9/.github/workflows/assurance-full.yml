name: Assurance Full Qualification

on:
  workflow_dispatch:
    inputs:
      candidate_sha:
        description: "Trusted full source commit; must equal the selected ref head"
        required: true
      target:
        description: "Factorio target line"
        required: true
        default: "2.1"
      prior_release:
        description: "Trusted runner path to the exact prior-release archive"
        required: true

permissions:
  contents: read

env:
  MIR_TRUST_CLASS: protected-release
  MIR_PROTECTED_ENVIRONMENT: release-candidate
  MIR_TRUSTED_RUNNER: self-hosted-windows

jobs:
  plan:
    name: verification-plan
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    outputs:
      f0: ${{ steps.matrices.outputs.f0 }}
      f1: ${{ steps.matrices.outputs.f1 }}
      f2: ${{ steps.matrices.outputs.f2 }}
      f3: ${{ steps.matrices.outputs.f3 }}
      f4: ${{ steps.matrices.outputs.f4 }}
      candidate_path: ${{ steps.matrices.outputs.candidate_path }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.candidate_sha }}
          fetch-depth: 0
      - name: Reject dirty, detached, or mismatched source
        shell: pwsh
        run: |
          if ('${{ inputs.candidate_sha }}' -ne '${{ github.sha }}') {
            throw 'candidate_sha must equal the selected workflow-dispatch ref head.'
          }
          if ((git rev-parse HEAD).Trim() -ne '${{ inputs.candidate_sha }}') {
            throw 'Candidate commit mismatch.'
          }
          if (git status --porcelain --untracked-files=all) {
            throw 'Candidate checkout is dirty.'
          }
      - name: Build exact candidate once
        shell: pwsh
        run: ./scripts/mir.ps1 assurance build --target '${{ inputs.target }}'
      - name: Materialize full verification plan
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
        run: ./scripts/mir.ps1 verify plan --target '${{ inputs.target }}' --profile full --factorio $env:FACTORIO_BIN --prior '${{ inputs.prior_release }}' --no-reuse --output out/verification-plan.json
      - name: Stage only the exact candidate and plan authority
        shell: pwsh
        run: |
          $plan = Get-Content -Raw -LiteralPath out/verification-plan.json | ConvertFrom-Json
          $stage = 'out/assurance-inputs'
          New-Item -ItemType Directory -Force -Path "$stage/out", "$stage/dist" | Out-Null
          Copy-Item -LiteralPath out/verification-plan.json -Destination "$stage/out/verification-plan.json"
          $plan.candidate_descriptor | ConvertTo-Json -Depth 20 |
            Set-Content -LiteralPath "$stage/out/candidate-descriptor.json" -Encoding utf8NoBOM
          $candidate = [string]$plan.candidate_descriptor.path
          if (-not (Test-Path -LiteralPath $candidate -PathType Leaf)) {
            throw "Planned candidate does not exist: $candidate"
          }
          Copy-Item -LiteralPath $candidate -Destination (Join-Path "$stage/dist" (Split-Path -Leaf $candidate))
      - id: matrices
        name: Split work into the enforced F0-F4 DAG
        shell: pwsh
        run: |
          $plan = Get-Content -Raw -LiteralPath out/verification-plan.json | ConvertFrom-Json
          "candidate_path=$([string]$plan.candidate_descriptor.path)" |
            Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          foreach ($layer in @('F0', 'F1', 'F2', 'F3', 'F4')) {
            $rows = @($plan.work | Where-Object { $_.layer -eq $layer })
            if ($rows.Count -eq 0) {
              $rows = @([ordered]@{
                test_id = '__none__'
                safe_test_id = "no-work-$layer"
                fingerprint = 'none'
                layer = $layer
              })
            }
            $name = $layer.ToLowerInvariant()
            "$name=$(@{ include = $rows } | ConvertTo-Json -Depth 10 -Compress)" |
              Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }
      - uses: actions/upload-artifact@v4
        with:
          name: mir-full-plan-and-candidate
          path: out/assurance-inputs
          if-no-files-found: error

  f0:
    name: F0 / ${{ matrix.test_id }}
    needs: plan
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.f0) }}
    concurrency:
      group: mir-${{ matrix.safe_test_id }}-${{ matrix.fingerprint }}
      cancel-in-progress: false
    steps: &worker-steps
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.candidate_sha }}
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: mir-full-plan-and-candidate
          path: .
      - name: Run exact fresh test instance
        if: matrix.test_id != '__none__'
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
        run: ./scripts/mir.ps1 verify run-one --target '${{ inputs.target }}' --plan out/verification-plan.json --test '${{ matrix.test_id }}' --fingerprint '${{ matrix.fingerprint }}' --factorio $env:FACTORIO_BIN --prior '${{ inputs.prior_release }}'
      - name: Stage only this worker's immutable capsule and structured results
        if: always() && matrix.test_id != '__none__'
        shell: pwsh
        run: |
          $delta = 'out/worker-delta'
          $capsule = "artifacts/assurance/evidence/${{ matrix.safe_test_id }}/${{ matrix.fingerprint }}"
          if (-not (Test-Path -LiteralPath $capsule -PathType Container)) {
            throw "Worker capsule directory is absent: $capsule"
          }
          $capsuleDestination = Join-Path $delta $capsule
          New-Item -ItemType Directory -Force -Path (Split-Path -Parent $capsuleDestination) | Out-Null
          Copy-Item -LiteralPath $capsule -Destination $capsuleDestination -Recurse
          $scenarioResult = "artifacts/validation/${{ matrix.safe_test_id }}.json"
          if (Test-Path -LiteralPath $scenarioResult -PathType Leaf) {
            $resultDestination = Join-Path $delta $scenarioResult
            New-Item -ItemType Directory -Force -Path (Split-Path -Parent $resultDestination) | Out-Null
            Copy-Item -LiteralPath $scenarioResult -Destination $resultDestination
          }
      - uses: actions/upload-artifact@v4
        if: always() && matrix.test_id != '__none__'
        with:
          name: mir-full-evidence-${{ matrix.safe_test_id }}
          path: out/worker-delta
          if-no-files-found: error

  f1:
    name: F1 / ${{ matrix.test_id }}
    needs: [plan, f0]
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.f1) }}
    concurrency:
      group: mir-${{ matrix.safe_test_id }}-${{ matrix.fingerprint }}
      cancel-in-progress: false
    steps: *worker-steps

  f2:
    name: F2 / ${{ matrix.test_id }}
    needs: [plan, f1]
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.f2) }}
    concurrency:
      group: mir-${{ matrix.safe_test_id }}-${{ matrix.fingerprint }}
      cancel-in-progress: false
    steps: *worker-steps

  f3:
    name: F3 / ${{ matrix.test_id }}
    needs: [plan, f2]
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.f3) }}
    concurrency:
      group: mir-${{ matrix.safe_test_id }}-${{ matrix.fingerprint }}
      cancel-in-progress: false
    steps: *worker-steps

  f4:
    name: F4 / ${{ matrix.test_id }}
    needs: [plan, f3]
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.f4) }}
    concurrency:
      group: mir-${{ matrix.safe_test_id }}-${{ matrix.fingerprint }}
      cancel-in-progress: false
    steps: *worker-steps

  verification-gate:
    name: verification-gate
    if: always()
    needs: [plan, f0, f1, f2, f3, f4]
    runs-on: [self-hosted, Windows]
    environment: release-candidate
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.candidate_sha }}
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: mir-full-plan-and-candidate
          path: .
      - uses: actions/cache/restore@v4
        with:
          path: artifacts/assurance/evidence
          key: mir-ledger-${{ runner.os }}-${{ inputs.target }}-${{ github.run_id }}
          restore-keys: |
            mir-ledger-${{ runner.os }}-${{ inputs.target }}-
      - name: Merge worker evidence
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          pattern: mir-full-evidence-*
          path: .
          merge-multiple: true
      - name: Aggregate canonical qualification gate
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
        run: ./scripts/mir.ps1 verify gate --target '${{ inputs.target }}' --plan out/verification-plan.json --factorio $env:FACTORIO_BIN --prior '${{ inputs.prior_release }}' --output artifacts/assurance/qualification-summary.json
      - name: Create protected candidate seal
        shell: pwsh
        env:
          FACTORIO_BIN: ${{ secrets.FACTORIO_BIN }}
        run: ./scripts/mir.ps1 assurance seal --target '${{ inputs.target }}' --plan out/verification-plan.json --factorio $env:FACTORIO_BIN --prior '${{ inputs.prior_release }}'
      - uses: actions/cache/save@v4
        if: always()
        with:
          path: artifacts/assurance/evidence
          key: mir-ledger-${{ runner.os }}-${{ inputs.target }}-${{ github.run_id }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: assurance-full-${{ inputs.target }}-${{ inputs.candidate_sha }}
          path: |
            out/candidate-descriptor.json
            ${{ needs.plan.outputs.candidate_path }}
            out/verification-plan.json
            artifacts/assurance/evidence-bundle.json
            artifacts/assurance/qualification-summary.json
            .mir/evidence/*-performance-regression.json
            .mir/evidence/*-manual-review-attestation.json
            .mir/evidence/qualifications/**
            .mir/evidence/candidate-seals/**
          if-no-files-found: warn
