---
title: "Post-3.1 Automatic Compiler Development Summary"
status: current
applies_to: "post-3.1 dev"
audience: release-manager
doc_type: release-plan
owner: mir-maintainers
last_reviewed: 2026-07-12
supersedes: []
superseded_by: []
---

# Post-3.1 Automatic Compiler Development Summary

The first automatic family compiler implementation is complete on `dev` from the immutable 3.1.0 behavior baseline. It does not rebuild or change the published 3.1.0 archive.

## Implemented

- All fixed streams compile into one pure `GenerationPlan`, validate as a complete set, then emit.
- Existing productivity-family adoption is also planned before mutation.
- RecipeFactV2 preserves typed variants, probability, catalysts, productivity exclusions, surfaces, caps, and source class.
- Shared relationship indexes cover recipe I/O/category/unlocks, item placement, entity types, effect ownership, labs, modules, upgrades, subgroups, and surfaces.
- Data-only FamilyRules attach structurally proven loader/logistics, mining-drill, furnace, inserter, power, and module recipes to six existing stable streams.
- Assembling-machine and lab manufacturing use predeclared stable generic-family identities and remain gated behind `safe-generate`; the default `safe-attach` mode emits neither technology.
- The golden plans lock all 70 released IDs plus the two reviewed 3.2.0 family IDs.
- Fixture-only competing policy moved out of production profiles and into validated `CompatibilityPack` `mod-data` packets.
- Capability resolver schema 2 uses `materialize` and `result`; resolver reporting is no longer described as prototype emission.
- Pipeline commands declare phases and dependencies, reject cycles, and fail on out-of-order execution.
- Direct-effect identity and numeric metadata have one domain authority.
- Scenario manifest schema 2 stores full target, kind, group, surface, and feature records.

## Verification

Structured evidence is `.mir/evidence/post-3.1-automatic-compiler-final-summary.json`.

- Run ID: `e61369cb-7a64-4d92-b727-e1bce9b90602`
- Result: `83/83` declared scenarios passed
- Factorio: `2.1.9`
- Duration: `725.402` seconds
- Validation package SHA-256: `EEB2567CBDB0B839943FCE9C3447F0FABBC206F47C90616879D762155851B3D2`
- Package content/source SHA-256: `F8C064661D109AE06E6AD9C5E00AF5A0F89DA7915523AE273B8FE7772049503A`
- Scenario manifest SHA-256: `662F7D4081EDEFD15CB6289105FEBD675F0420AACE05EE8ECD58196D3FFE342E`
- Static, package determinism, base, Space Age, ownership, adoption, compatibility-pack, upgrade-adjacent configuration-change, and golden-ID gates passed.

The runtime record names pre-rebase source commit `ea35733e230532a248d71147dd06ffb53d802af4`. During final synchronization, `dev` was rebased onto the concurrently advanced `origin/dev`; the validated source tree is byte-identical to rebased commit `67cef34c22093a8d0a8576a563c534edcb6e8c41` (`git diff --quiet` passed).

## Remaining Release Work

This is an integration-validated development line, not a published 3.2.0 release candidate. Before a release, add performance budgets for the larger diagnostic/fact payload, run selected real-mod compatibility packs, review automatic balance changes, and complete save-upgrade and interactive UI review for the eventual version.

## Plan And Decision Schema 2 Gate

Commit `7e2fc6e86474e9460bf7c368092121ba432465e0` added deterministic fact, rule, target-profile, and plan fingerprints; explicit target, effect, owner, science, lab, prerequisite, and loop-safety gates; plan validation summaries; and typed decision confidence. Structured run `f037c887-593c-4daf-b302-2c64656b0051` passed all `83` declared Factorio 2.1 scenarios in `621.677` seconds. Evidence is `.mir/evidence/3.2.0-plan-v2-gate-summary.json`.
