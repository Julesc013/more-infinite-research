---
title: "3.0.0 Validation Summary"
status: current
applies_to: "3.0.0"
audience: release-manager
doc_type: release-plan
owner: mir-maintainers
last_reviewed: 2026-07-09
supersedes: []
superseded_by: []
---

# 3.0.0 Validation Summary

Updated: 2026-07-09

This is the concise release evidence record for MIR `3.0.0`. The full local
artifact tree is intentionally not committed.

## Release Snapshot

- Package-visible source state: final `3.0.0` settings-wording polish tree
- Publish-candidate gate artifact:
  `artifacts\release-targeted-20260708-183311`
- Release version: `3.0.0`
- Factorio line: `2.1`
- Final package: `dist/more-infinite-research_3.0.0.zip`
- Package size: `273201` bytes
- Package SHA-256:
  `4C4C717BCB295822879653BC7C56D098BFED3508E59266618807C586E023301B`

## Commands

```powershell
.\scripts\mir.ps1 legacy inventory --check
.\scripts\Invoke-MIRValidation.ps1 -ArchitectureOnly
.\scripts\Invoke-MIRValidation.ps1 -StaticOnly
.\scripts\Invoke-MIRValidation.ps1 -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe"
.\scripts\Invoke-MIRReleaseTargetedGate.ps1 -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe" -FactorioLine "2.1" -LocalModDir "C:\Projects\Factorio\testmods_2.1" -RepairSmokeModNames "big-mining-drill","biolabs-in-space","aai-containers","aai-industry","aai-loaders","equipment-gantry","FluidMustFlow","jetpack","robot_attrition" -RepresentativeScenarioName "local-2-1-bz-suite-space-age" -ManualScenariosPath "fixtures\compat-matrix\local-library-scenarios.json" -AuditFactorioVersions "2.1" -PackageOutputDir "build\release-targeted-dist" -NoGitPull
.\scripts\Build-MIRPackage.ps1
# Final ATAN release-dist isolation used the rebuilt dist archive, isolated write-data roots, and C:\Projects\Factorio\testmods_2.1.
git diff --check
```

## Verdicts

- Legacy inventory: passed with zero old shipped shim paths, zero old imports,
  zero old root helper files, zero runtime Lua files under `control/`, zero
  direct prototype access outside allowed layers, and zero generated streams
  without manifest rows.
- Static validation: passed, including docs, governance manifests, architecture
  boundaries, settings visibility, policy and claim linting, changelog syntax,
  and package hygiene.
- Runtime fixture validation: passed on the Factorio `2.1` binary.
- Targeted release gate: passed at
  `artifacts\release-targeted-20260708-183311`.
- Package hygiene: final `dist` archive contains `115` entries and zero
  forbidden repository-only paths.
- Targeted ATAN release-dist isolation passed at
  `artifacts\atan-release-dist-validation`: upstream-only `atan-ash_2.2.1`
  and `atan-nuclear-science_0.3.3` fail on the tested Factorio `2.1`
  schema checks, while the same zips load with the final MIR dist package and
  emit the exact repair log lines.
- This evidence record may receive docs-only updates after the package-visible
  release commit. The package SHA-256 above identifies the release payload.

## RC Recheck - 2026-07-09

Fresh selected-mod RC checks were run without overnight profiles or broad local
library sweeps:

```powershell
.\scripts\Invoke-MIRValidation.ps1 -StaticOnly
.\scripts\Invoke-MIRValidation.ps1 -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe"
.\scripts\Invoke-MIRExtendedTests.ps1 -Tier LocalModZips -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe" -FactorioLine 2.1 -LocalModZipDirs "C:\Projects\Factorio\testmods_2.1" -LocalModLibraryDirs "C:\Projects\Factorio\testmods_2.1" -LocalModNames "atan-air-scrubbing","atan-ash","atan-nuclear-science","aai-containers","aai-industry","aai-loaders","big-mining-drill","biolabs-in-space","equipment-gantry","FluidMustFlow","jetpack","robot_attrition" -Offline -CollectAll -FailOnAuditFailures -ScenarioTimeoutSeconds 900 -OutputRoot ".\tmp\rc-local-zips"
.\scripts\Invoke-MIRExtendedTests.ps1 -Tier LocalLibraryScenarios -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe" -FactorioLine 2.1 -ManualScenariosPath "fixtures\compat-matrix\local-library-scenarios.json" -ScenarioNames "local-2-1-bz-suite-space-age" -LocalModZipDirs "C:\Projects\Factorio\testmods_2.1" -LocalModLibraryDirs "C:\Projects\Factorio\testmods_2.1" -Offline -FailOnAuditFailures -ScenarioTimeoutSeconds 900 -OutputRoot ".\tmp\rc-bz-scenario"
.\scripts\Invoke-MIRExtendedTests.ps1 -Tier AuditSmoke -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe" -FactorioLine 2.1 -FailOnAuditFailures -ScenarioTimeoutSeconds 900 -OutputRoot ".\tmp\rc-audit-smoke"
```

Results:

- Static validation passed.
- Runtime fixture validation passed on the Factorio `2.1` binary, including
  Air Scrubbing, ATAN Ash, ATAN Nuclear Science, AAI Loaders, Big Mining Drill,
  and generated-stream integrity fixtures.
- Selected local zip checks passed for `atan-air-scrubbing`, `atan-ash`,
  `atan-nuclear-science`, `aai-containers`, `aai-industry`, `aai-loaders`,
  `big-mining-drill`, `biolabs-in-space`, `equipment-gantry`, `FluidMustFlow`,
  `jetpack`, and `robot_attrition`.
- The selected local zip lane produced zero dependency failures and zero
  unexpected grouped failures.
- The `local-2-1-bz-suite-space-age` representative scenario passed with zero
  dependency failures and zero unexpected grouped failures.
- The Space Age audit-smoke lane passed with zero unexpected grouped failures.
- The compatibility-audit staging helper was fixed to exclude repository-only
  development and temporary paths from unpacked load-test mod copies; this is a
  validation-tooling fix and does not change the shipped mod payload.
- The transient `tmp\rc-*` output roots were deleted after results were
  recorded.

## Settings Wording Polish Recheck - 2026-07-09

After the player-facing settings label pass, these focused checks passed:

```powershell
.\scripts\Test-MIRLocales.ps1
.\scripts\Invoke-MIRValidation.ps1 -StaticOnly
.\scripts\Build-MIRPackage.ps1
.\scripts\Invoke-MIRValidation.ps1 -FactorioBin "C:\Program Files\Steam\steamapps\common\Factorio\bin\x64\factorio.exe"
git diff --check
```

The release package was rebuilt as `273201` bytes with SHA-256
`4C4C717BCB295822879653BC7C56D098BFED3508E59266618807C586E023301B`.
The archive still contains `115` entries and zero forbidden repository-only
paths. Transient validation output under `build\validation-dist` was removed
after the final package rebuild.

## Regression Comparison

Compared against `artifacts\regression-20260707-final-current-fullrepair`:

| Lane | Rows | Observation hash | Claim hash | Unexpected |
| --- | ---: | --- | --- | ---: |
| strict audit smoke | `814 -> 814` | unchanged | unchanged | `0 -> 0` |
| repair smokes | `1820 -> 1820` | unchanged | unchanged | `0 -> 0` |
| representative local scenario | `924 -> 924` | unchanged | unchanged | `0 -> 0` |

The targeted gate also wrote detailed planner diffs under
`artifacts\release-targeted-20260708-183311`.

## Local Coverage

The final targeted gate used these local repair-smoke roots:

- `big-mining-drill`
- `biolabs-in-space`
- `aai-containers`
- `aai-industry`
- `aai-loaders`
- `equipment-gantry`
- `FluidMustFlow`
- `jetpack`
- `robot_attrition`

The representative local scenario was `local-2-1-bz-suite-space-age`.

## Known Non-Blockers

- `FACTORIO_TOKEN` was not set in this environment, so portal-backed
  full-catalog checks were not run.
- Local supported-zip isolation reproduced upstream Factorio `2.1`
  recipe-schema failures for `atan-ash_2.2.1` and
  `atan-nuclear-science_0.3.3` without MIR. MIR `3.0.0` applies exact-version
  loader-schema repairs for those zips when they are loaded with MIR; the
  repair does not broaden MIR's productivity claims.
- `prototypes/streams/` remains the documented declarative stream-data
  exception for `3.0.0`; active implementation stays under `prototypes/mir/`.
